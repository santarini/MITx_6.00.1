import math

def polysum(n, s):
    numerator = 0.25 * n * (s**2)
    denominator = math.tan(math.pi/n)
    areaOfPoly = numerator/denominator
    perimeter = n * s
    return(round(areaOfPoly + (perimeter**2),4))
